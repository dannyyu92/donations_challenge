<%= form_for [current_user, @donation] do |f| %>
  <% if @donation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@donation.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @donation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <%= f.label :title %><br />
  <%= f.text_field :title, placeholder: "Title", id: "donation_title" %><br />
  <%= f.label :description %><br />
  <%= f.text_area :description, placeholder: "Description", id: "donation_description" %><br />
  <%= f.submit "Submit" %>
<% end %>